language: node_js
node_js:
  - lts/*
install:
  - yarn install
script:
  - yarn docs:build
deploy:
  provider: pages
  skip_cleanup: false
  local_dir: docs/.vuepress/dist
  github_token: $gh_tci_token
  keep_history: true
  on:
    branch: master